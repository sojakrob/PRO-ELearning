@model ELearning.Models.Data.NewGroupModel


<div class="editor-group">
    <div class="editor-label">
        @Html.LabelFor(model => model.Name)
    </div>
    <div class="editor-field">
        @Html.EditorFor(model => model.Name)
        @Html.ValidationMessageFor(model => model.Name)
    </div>
    <div class="clear"></div>
</div>

<div class="editor-group">
    <div class="editor-label">
        @Html.LabelFor(model => model.SupervisorID)
    </div>
    <div class="editor-field">
        <select name="SupervisorID">
            @foreach (var lector in Model.PossibleSupervisors)
            {
                <option value="@lector.ID">@lector.Name</option>
            }
        </select>
    </div>
</div>

@*<div class="editor-group">
    <div class="editor-label">
        UserType
    </div>
    <div class="editor-field">
        @Html.DropDownListFor(
            model => model.Type.ID,
            new SelectList(((ICollection<ELearning.Models.Data.UserTypeModel>)ViewBag.UserTypes), "ID", "Name", Model == null ? ViewBag.DefaultUserType : Model.Type.ID)
            )
        @Html.ValidationMessageFor(model => model.Type.ID)
    </div>
    <div class="clear"></div>
</div>
*@