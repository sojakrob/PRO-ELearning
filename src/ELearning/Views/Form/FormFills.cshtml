@model ELearning.Models.FormFillsModel

@{
    ViewBag.Title = "Form fills";
}

<h2>@Model.Name</h2>



<table class="datagrid">

    <tr>
        <th>Solver</th>
        <th>Times filled</th>
    </tr>

    @foreach (var forms in Model.FormInstances.GroupBy(i => i.Solver.ID).ToList())
    {
        <tr>
            <td>@forms.First().Solver.Name</td>
            <td>@forms.Count()</td>
        </tr>
    }

</table>

